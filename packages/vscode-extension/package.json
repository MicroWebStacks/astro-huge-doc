{
  "name": "microwebstacks-docs-preview",
  "displayName": "MicroWebStacks Docs Preview",
  "description": "Preview large Markdown documentation sets inside VS Code: file tree, outline, diagrams, and tables.",
  "version": "0.0.7",
  "icon": "icon.png",
  "engineVersion": "0.0.3",
  "publisher": "microwebstacks",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MicroWebStacks/astro-huge-doc.git",
    "directory": "packages/vscode-extension"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "microwebstacks.previewDocs",
        "title": "MicroWebStacks: Preview Docs in VS Code"
      },
      {
        "command": "microwebstacks.openDocsInBrowser",
        "title": "MicroWebStacks: Open Docs in Browser"
      },
      {
        "command": "microwebstacks.restartDocsPreviewServer",
        "title": "MicroWebStacks: Restart Docs Preview Server"
      },
      {
        "command": "microwebstacks.stopDocsPreviewServer",
        "title": "MicroWebStacks: Stop Docs Preview Server"
      }
    ],
    "configuration": {
      "title": "MicroWebStacks Docs Preview",
      "properties": {
        "microwebstacks.preview.engineSource": {
          "type": "string",
          "enum": [
            "auto",
            "local",
            "registry"
          ],
          "enumDescriptions": [
            "Use enginePath if set (or a source checkout when the extension runs from one), else a previously installed engine, else install from the npm registry.",
            "Only use enginePath (or a source checkout when the extension runs from one). Never contacts the npm registry (fully offline).",
            "Skip local discovery and use (or install) the published @microwebstacks/md-render engine."
          ],
          "default": "auto",
          "description": "Where the docs rendering engine is sourced from. For an installed extension, local discovery requires enginePath pointing at an astro-huge-doc checkout; 'auto' otherwise installs the engine from the npm registry on first use."
        },
        "microwebstacks.preview.enginePath": {
          "type": "string",
          "default": "",
          "description": "Path to an astro-huge-doc repository checkout used as the engine. Highest priority in every engineSource mode. Only needed when the extension cannot infer the engine path."
        },
        "microwebstacks.preview.docsRoot": {
          "type": "string",
          "default": "",
          "description": "Optional path to the documentation root inside the workspace. Defaults to manifest output.content when manifest.yaml exists, otherwise the workspace folder."
        },
        "microwebstacks.preview.krokiServer": {
          "type": "string",
          "default": "http://localhost:18000",
          "markdownDescription": "Base URL of the Kroki-compatible server that renders diagrams (mermaid, plantuml, blockdiag). The preview POSTs diagram source to this URL. Use a local Docker Kroki (`http://localhost:18000`), the public service (`https://kroki.io`), or your organization's internal endpoint. Leave blank to fall back to the workspace `.env` or environment."
        }
      }
    }
  }
}
