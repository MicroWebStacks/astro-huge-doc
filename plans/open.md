# Open Plans

Plan packets with work still outstanding. See each folder for details.

| Plan | Date | Status | Summary |
| --- | --- | --- | --- |
| [2026-07-25-homesmartmesh-parity](2026-07/25/homesmartmesh-parity/) | 2026-07-25 | Phase 2/5 done | Publishing the HomeSmartMesh content set as a static GitHub Pages site at parity with <https://homesmartmesh.github.io/>. **Phases 0-2 complete**: both static targets build, shared path-only identity preserves URL-safe GitHub filenames, the isolated content migration collects with zero unresolved references, and the full artifact now renders the authored galleries, two YouTube embeds, twelve external GLB viewers, two XLSX tables, and text-form details directives. The expanded suite passes 97/97 and the full static build emits 115 pages. The publishing-action cutover remains outside this packet. **Phases 4 and 5 are complete**: the static artifact no longer bakes in the extension-preview surface (the repo `.env` had it on), chrome collapses behind one shared arrow with sticky state, favicons and an optional site footer are wired, `pages.yml` publishes on demand, and a render-parity harness guards a recorded 115-page baseline. `WP-24` found and fixed 95 internal links that lost the base prefix under a sub-path deployment. **Phase 3 is partial**: `WP-13` fetches `public/data` and the loose favicons/`.nojekyll` through a new additive `files:` manifest entry, so all 14 download buttons resolve; `WP-14`/`WP-21`/`WP-23`/`WP-25` are the only work left. The suite passes 120/120, the full static build emits 115 pages, lite 76, SSR builds clean. The sparse-C3 review observation was closed as accepted-as-is (`R-18`). |
| [2026-07-30-interactive-graph-layout](2026-07/30/interactive-graph-layout/) | 2026-07-30 | Phase 5/6 — vis-network selected | vis-network/ForceAtlas2-based physics is now the sole lazy-loaded graph renderer. Its fixed-width slider shows stable global counts; `+N`/`−` satellites manage local branches, advancing the slider when they complete a whole level. The collision-free local-count/Clear region and hover `Open ↗` action make branch and navigation actions explicit. Automated and production-build validation pass; live browser interaction and responsive verification remain. |
